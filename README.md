# TechStore - Интернет-магазин техники

Полнофункциональный интернет-магазин техники с админ-панелью, созданный с использованием Node.js, Express, SQLite, React и Redux.

## 🚀 Быстрый запуск

### Предварительные требования

- Node.js (версия 16 или выше)
- npm или yarn

### Автоматический запуск (рекомендуется)

#### Windows (PowerShell)
```powershell
.\start.ps1
```

#### Windows (CMD)
```cmd
start.bat
```

#### Linux / macOS
```bash
chmod +x start.sh
./start.sh
```

Скрипт автоматически:
- ✅ Проверит наличие Node.js и npm
- ✅ Создаст файл `.env` из `env.example` (если отсутствует)
- ✅ Установит все зависимости
- ✅ Инициализирует базу данных
- ✅ Запустит оба сервера (backend и frontend)
- ✅ Откроет браузер с приложением

### Ручной запуск

Если вы предпочитаете запускать серверы вручную:

#### 1. Установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

#### 2. Настройка окружения

Создайте файл `.env` в директории `backend/`:
```bash
cp backend/env.example backend/.env
```

#### 3. Инициализация базы данных

```bash
cd backend
npm run init-db
```

Эта команда создаст:
- Базу данных SQLite
- Таблицы для пользователей, товаров, заказов и т.д.
- Тестового администратора (admin@example.com / admin123)
- Примеры категорий и товаров

#### 4. Запуск серверов

**Терминал 1 - Backend:**
```bash
cd backend
npm start
```
Сервер запустится на `http://localhost:5001`

**Терминал 2 - Frontend:**
```bash
cd frontend
PORT=3001 npm start
```
Приложение откроется на `http://localhost:3001`

## 📋 Тестовые данные

### Админ аккаунт
- **Email:** admin@example.com
- **Пароль:** admin123

### Тестовые товары
После инициализации базы данных будут созданы примеры товаров в категориях:
- Смартфоны (iPhone 15 Pro, Samsung Galaxy S24)
- Ноутбуки (MacBook Pro 16")
- Аксессуары (AirPods Pro)

## 🛠 API Эндпоинты

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/profile` - Профиль пользователя

### Товары
- `GET /api/products` - Получить все товары (с фильтрами)
- `GET /api/products/:id` - Получить товар по ID
- `POST /api/products` - Создать товар (админ)
- `PUT /api/products/:id` - Обновить товар (админ)
- `DELETE /api/products/:id` - Удалить товар (админ)

### Категории
- `GET /api/categories` - Получить все категории
- `GET /api/categories/tree` - Получить дерево категорий
- `POST /api/categories` - Создать категорию (админ)

### Отзывы
- `GET /api/reviews/product/:productId` - Отзывы товара
- `POST /api/reviews` - Создать отзыв
- `PUT /api/reviews/:id` - Обновить отзыв

### Заказы (в разработке)
- `GET /api/orders/my-orders` - Заказы пользователя
- `POST /api/orders` - Создать заказ

### Админ-панель
- `GET /api/admin/stats` - Статистика
- `GET /api/admin/users` - Управление пользователями
- `GET /api/admin/products` - Управление товарами

## 🗂 Структура проекта

```
app_store/
├── backend/                    # Серверная часть
│   ├── config/
│   │   └── database.js        # Конфигурация SQLite
│   ├── models/                # Модели данных
│   │   ├── User.js
│   │   ├── Product.js
│   │   ├── Category.js
│   │   ├── Review.js
│   │   └── ...
│   ├── routes/                # API маршруты
│   ├── middleware/            # Middleware
│   ├── scripts/               # Скрипты инициализации
│   └── server.js              # Главный серверный файл
├── frontend/                  # Клиентская часть
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   ├── pages/            # Страницы
│   │   ├── features/         # Redux slices
│   │   ├── store/            # Redux store
│   │   └── services/         # API сервисы
│   └── ...
└── README.md
```

## 🔧 Скрипты

### Скрипты запуска проекта
- `start.ps1` - PowerShell скрипт для Windows (рекомендуется)
- `start.bat` - Batch скрипт для Windows (CMD)
- `start.sh` - Bash скрипт для Linux/macOS

### Backend
```bash
npm start          # Запуск сервера
npm run dev        # Запуск в режиме разработки (с nodemon)
npm run init-db    # Инициализация базы данных
```

### Frontend
```bash
npm start          # Запуск dev сервера
npm run build      # Сборка для production
npm test           # Запуск тестов
```

## 🌐 Переменные окружения

Создайте файл `.env` в директории `backend/`:

```env
# Сервер
PORT=5000
NODE_ENV=development

# База данных
DB_PATH=./database.sqlite

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email (для будущих интеграций)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-password

# Frontend
FRONTEND_URL=http://localhost:3000

# Админ
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
```

## 🎯 Функциональность MVP

### ✅ Реализовано
- [x] Базовая структура проекта
- [x] SQLite база данных с моделями
- [x] Аутентификация пользователей (JWT)
- [x] API для товаров, категорий, отзывов
- [x] React frontend с Redux
- [x] Базовые страницы (главная, товары, вход/регистрация)
- [x] Адаптивный дизайн с Tailwind CSS
- [x] Админ-панель (базовая)

### 🚧 В разработке
- [ ] Детальная страница товара
- [ ] Корзина и оформление заказа
- [ ] Полнофункциональная админ-панель
- [ ] Интеграция платежей (Яндекс.Касса)
- [ ] Email уведомления
- [ ] Загрузка изображений товаров

## 🐛 Устранение неполадок

### Ошибка подключения к базе данных
```bash
cd backend
rm database.sqlite
npm run init-db
```

### Проблемы с зависимостями
```bash
# Backend
cd backend
rm -rf node_modules package-lock.json
npm install

# Frontend
cd ../frontend
rm -rf node_modules package-lock.json
npm install
```

### Порт уже занят
Измените порт в `.env` файле или завершите процесс, использующий порт 5000/3000.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера в терминале
2. Убедитесь, что все зависимости установлены
3. Проверьте корректность переменных окружения
4. Попробуйте перезапустить сервер

## 📄 Лицензия

Этот проект создан для образовательных целей.
