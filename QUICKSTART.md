# 🚀 Быстрый старт TechStore

## Автоматический запуск (рекомендуется)

### Windows

#### PowerShell (рекомендуется)
```powershell
.\start.ps1
```

#### Командная строка (CMD)
```cmd
start.bat
```

### Linux / macOS
```bash
chmod +x start.sh
./start.sh
```

## Что делает скрипт?

1. ✅ Проверяет наличие Node.js и npm
2. ✅ Создаёт файл `.env` из `env.example` (если отсутствует)
3. ✅ Устанавливает все зависимости (backend и frontend)
4. ✅ Инициализирует базу данных с тестовыми данными
5. ✅ Запускает backend сервер на порту 5001
6. ✅ Запускает frontend сервер на порту 3001
7. ✅ Автоматически открывает браузер через 10 секунд

## После запуска

- **Frontend:** http://localhost:3001
- **Backend API:** http://localhost:5001
- **Тестовый админ:**
  - Email: `admin@example.com`
  - Пароль: `admin123`

## Остановка серверов

### Windows (PowerShell)
- Закройте окна серверов или нажмите любую клавишу в главном окне

### Windows (CMD)
- Закройте окна серверов или нажмите Ctrl+C в каждом окне

### Linux / macOS
- Нажмите Ctrl+C в терминале

## Устранение проблем

### Ошибка "Node.js не установлен"
Установите Node.js 16+ с https://nodejs.org

### Порт уже занят
1. Найдите процесс, использующий порт:
   ```powershell
   # Windows
   netstat -ano | findstr ":5001"
   netstat -ano | findstr ":3001"
   ```
2. Завершите процесс или измените порты в `.env` файле

### Ошибка "react-scripts не является внутренней или внешней командой"

Эта ошибка означает, что зависимости frontend не установлены или установлены некорректно.

**Решение:**
```powershell
# Windows PowerShell
cd frontend
Remove-Item -Recurse -Force node_modules, package-lock.json
npm install
```

```bash
# Linux / macOS
cd frontend
rm -rf node_modules package-lock.json
npm install
```

Или просто запустите скрипт `start.ps1` / `start.bat` / `start.sh` заново - он автоматически проверит и переустановит зависимости при необходимости.

### Ошибка установки зависимостей
```bash
# Удалите node_modules и переустановите
cd backend
rm -rf node_modules package-lock.json
npm install

cd ../frontend
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с базой данных
```bash
cd backend
rm database.db database.sqlite
npm run init-db
```

## Ручной запуск

Если автоматический скрипт не работает, следуйте инструкциям в [README.md](README.md)
